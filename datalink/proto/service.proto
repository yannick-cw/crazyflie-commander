syntax = "proto3";
import "telemetry.proto";
import "google/protobuf/empty.proto";

package downlink;

service StreamTelemetry {
  // extend to carry different messages at different rates: pos state data, health data (bat, maybe armed or so), mission status
  rpc StreamTelemetry (google.protobuf.Empty) returns (stream Message) {}
  // new stream with grid
}
